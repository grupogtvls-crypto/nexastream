'use client';
import Link from 'next/link';
import { FormEvent, useState } from 'react';
import { useRouter } from 'next/navigation';
import { SiteHeader } from '../../components/site-header';
export default function SignupPage() { const router = useRouter(); const [name, setName] = useState(''); const [email, setEmail] = useState(''); const [phone, setPhone] = useState(''); const [password, setPassword] = useState(''); const [error, setError] = useState(''); const [loading, setLoading] = useState(false); async function handleSubmit(e: FormEvent) { e.preventDefault(); setError(''); setLoading(true); try { const response = await fetch(`${process.env.NEXT_PUBLIC_API_URL}/api/auth/register`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, credentials: 'include', body: JSON.stringify({ name, email, phone, password }) }); if (!response.ok) throw new Error('Falha'); router.push('/login'); } catch { setError('Não foi possível criar a conta'); } finally { setLoading(false); } } return <main className="page-bg"><SiteHeader /><div className="container" style={{ paddingTop: 60, paddingBottom: 60 }}><div className="grid-2"><div className="card" style={{ padding: 28 }}><h1>Criar conta</h1><form onSubmit={handleSubmit} style={{ display: 'grid', gap: 14, marginTop: 20 }}><input className="input" value={name} onChange={(e) => setName(e.target.value)} placeholder="Nome completo" /><input className="input" value={email} onChange={(e) => setEmail(e.target.value)} placeholder="E-mail" /><input className="input" value={phone} onChange={(e) => setPhone(e.target.value)} placeholder="Telefone" /><input className="input" type="password" value={password} onChange={(e) => setPassword(e.target.value)} placeholder="Senha" />{error ? <p style={{ color: '#f87171', margin: 0 }}>{error}</p> : null}<button className="btn-primary" disabled={loading}>{loading ? 'Criando...' : 'Criar conta'}</button></form><div style={{ display: 'grid', gap: 12, marginTop: 18 }}><a className="btn-secondary" href={`${process.env.NEXT_PUBLIC_API_URL}/api/auth/google`}>Cadastrar com e-mail do Google</a><a className="btn-secondary" href={`${process.env.NEXT_PUBLIC_API_URL}/api/auth/facebook`}>Cadastrar com conta do Facebook</a></div><p style={{ color: '#94a3b8', marginTop: 18 }}>Já possui conta? <Link href="/login" style={{ color: '#67e8f9', fontWeight: 700 }}>Entrar</Link></p></div><div className="card" style={{ padding: 28 }}><h2>Benefícios do cadastro</h2><p style={{ color: '#94a3b8' }}>Acesso a downloads, dispositivos e área do cliente.</p></div></div></div></main>; }
