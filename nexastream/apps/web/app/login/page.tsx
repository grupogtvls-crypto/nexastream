'use client';
import Link from 'next/link';
import { FormEvent, useState } from 'react';
import { useRouter } from 'next/navigation';
import { SiteHeader } from '../../components/site-header';
export default function LoginPage() { const router = useRouter(); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [error, setError] = useState(''); const [loading, setLoading] = useState(false); async function handleSubmit(e: FormEvent) { e.preventDefault(); setError(''); setLoading(true); try { const response = await fetch(`${process.env.NEXT_PUBLIC_API_URL}/api/auth/login`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, credentials: 'include', body: JSON.stringify({ email, password }) }); if (!response.ok) throw new Error('Falha'); const data = await response.json(); localStorage.setItem('nexastream_token', data.accessToken); router.push('/cliente'); } catch { setError('E-mail ou senha inválidos'); } finally { setLoading(false); } } return <main className="page-bg"><SiteHeader /><div className="container" style={{ paddingTop: 60, paddingBottom: 60 }}><div className="grid-2"><div className="card" style={{ padding: 28 }}><h1>Entrar na NexaStream</h1><form onSubmit={handleSubmit} style={{ display: 'grid', gap: 14, marginTop: 20 }}><input className="input" value={email} onChange={(e) => setEmail(e.target.value)} placeholder="Seu e-mail" /><input className="input" value={password} onChange={(e) => setPassword(e.target.value)} type="password" placeholder="Sua senha" />{error ? <p style={{ color: '#f87171', margin: 0 }}>{error}</p> : null}<button className="btn-primary" disabled={loading}>{loading ? 'Entrando...' : 'Entrar'}</button></form><div style={{ display: 'grid', gap: 12, marginTop: 18 }}><a className="btn-secondary" href={`${process.env.NEXT_PUBLIC_API_URL}/api/auth/google`}>Entrar com Google</a><a className="btn-secondary" href={`${process.env.NEXT_PUBLIC_API_URL}/api/auth/facebook`}>Entrar com Facebook</a></div><p style={{ color: '#94a3b8', marginTop: 18 }}>Ainda não possui conta? <Link href="/cadastro" style={{ color: '#67e8f9', fontWeight: 700 }}>Criar cadastro</Link></p></div><div className="card" style={{ padding: 28 }}><h2>Acesso premium</h2><p style={{ color: '#94a3b8' }}>Conecte-se ao ecossistema NexaStream.</p></div></div></div></main>; }
